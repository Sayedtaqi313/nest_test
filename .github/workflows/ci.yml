name: Practice 1

on:
  push:

jobs:
  hello:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - run: pwd

      - run: ls -la
      - run: echo "Hello Mahdi"


name: My first workflow

on:
  push:

jobs:
  first:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - run: node -v

      - run: npm -v